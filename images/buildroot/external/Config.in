# BR2_EXTERNAL menu for NeuronEdge Enclave. The base configuration ships no extra packages
# yet (the guest agent is wired in via a post-build script for the
# first iteration). When we add a Buildroot package for the agent,
# its Config.in lands here.
